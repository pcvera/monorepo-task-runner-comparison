// This comment will be sddtripped from the output, modifying it should not trigger a rebuild because of the stripped comments output being the same.
export default function sampleInput() {
  return "sampleInput";
}
