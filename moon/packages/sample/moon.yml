language: "javascript"
layer: "library"

tasks:
  build:
    command: node
    args:
      - scripts/dummy-build.cjs
    deps:
      - "stripped:build"
    inputs:
      - "src/**/*.json"
      - "scripts/**/*.cjs"
    outputs:
      - "dist"
