{
  "name": "moon",
  "private": true,
  "scripts": {
    "build": "moon run :build",
    "reset": "moon clean --all && rm -rf packages/stripped/build packages/stripped/dist packages/sample/build packages/sample/dist"
  },
  "packageManager": "pnpm@10.10.0",
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@moonrepo/cli": "^2.2.3"
  }
}
