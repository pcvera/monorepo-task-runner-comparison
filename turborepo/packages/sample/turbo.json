{
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["src/**/*.json", "scripts/**/*.cjs", "../stripped/build/stripped-comments/**"],
      "outputs": ["dist/**"]
    }
  }
}
